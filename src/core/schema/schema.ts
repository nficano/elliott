// ComponentSchema — runtime equivalent of type() (TDD §1, §2). Manifests
// reference their schema by (kind, apiVersion, digest); they never embed it.
//
// Deferred to M0: schema registry + SchemaRef resolution.

export {};
