// Schema-backed behavior contracts — TDD §1. Protocol names use
// <domain>.<interface> (§15b): message.source, tool.executor, policy.decider,
// model.inference, memory.reader, composition.members, …
//
// Deferred to M0: protocol descriptor validation + structural conformance checks.

export {};
