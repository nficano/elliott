// Schema-backed behavior contracts — TDD §7.3. Protocol names use
// <domain>.<interface> (§7.17b): message.source, tool.executor, policy.decider,
// model.inference, memory.reader, composition.members, …

export type { ProtocolDescriptor, ProtocolId } from "../types";
