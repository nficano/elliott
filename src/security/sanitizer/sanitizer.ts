// Declassification — TDD §7. Deterministic sanitizers first, TLE as a second
// gate; sanitizer decisions cache by full policy input (G19).
//
// Deferred to P2: sanitizer pipeline layers 1–3 + decision caching (§7a).

export {};
