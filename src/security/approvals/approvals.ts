// Approvals — TDD §8d. Deferred grants activate after JIT operator approval
// on first use; activation bumps the session epoch (§1).
//
// Deferred to M5: approval queue + posture-scoped remembering (§0e).

export {};
