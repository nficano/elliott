// Secrets — TDD §8c. Secrets are opaque references (secret://…) and never
// enter model context (core invariant 5).
//
// Deferred to M5: secret reference resolution at the broker boundary.

export {};
