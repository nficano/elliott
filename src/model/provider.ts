// ModelProviderProtocol host-side plumbing — TDD §4. The kernel owns profile
// intent, classification/residency, budgets, prompts, tools, approvals;
// providers own API translation, aliases, credentials, failover, catalogs.
//
// Deferred to M4: provider component binding + push-based health (§4b).

export {};
