// Model profiles and use policy — TDD §5a, §5e. Reserved profiles are stable
// semantic contracts (fast/balanced/deep, capability-only ordering); the
// `uses` map resolves an activity name to a profile before dispatch.
//
// Deferred to M4: profile ceiling checks and governed escalation chains.

export {};
