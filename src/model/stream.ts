// Model stream event plumbing — TDD §4, §8a (incremental inspection of
// streamed tool calls happens broker-side, not here).
//
// Deferred to M4: stream event normalization across providers.

export {};
