// Standard Markdown documents per kind (SKILL.md, AGENT.md, …) — TDD §9,
// §16b. Hardening rules for prompt-visible text are specified in §9.
//
// Deferred to M2: markdown parsing + prompt-visibility rules.

export {};
