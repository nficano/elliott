# Elliott-authored type stubs for the AGPL Darwinian Evolver API surface used by
# the companion. Real sources ship only inside the sealed companion image; these
# stubs exist so local basedpyright can resolve imports without vendoring AGPL.
