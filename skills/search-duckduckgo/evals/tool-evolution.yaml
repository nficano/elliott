- id: code-tool-contract
  groupId: defect-tool-contract
  input:
    reproduction: Register the DuckDuckGo search tool and inspect its public operation and input schema.
  expected:
    behavior: Preserve the duckduckgo_search operation name and required string query parameter.
  rubric: The component tool contract remains compatible with existing callers.
  classification: internal
  sourceDigests: [sha256:duckduckgo-tool-reproduction-v1]
  timeoutMilliseconds: 120000
  maximumCostUsd: 0
  allowedEffects: [process.execute:test-allowlist]
- id: code-tool-request
  groupId: defect-tool-request
  input:
    reproduction: Execute a search against a deterministic HTTP fixture and capture the outbound destination and query.
  expected:
    behavior: Send the encoded query only to the declared DuckDuckGo HTML endpoint and return parsed safe results.
  rubric: Preserve declared egress and deterministic parser integration.
  classification: internal
  sourceDigests: [sha256:duckduckgo-tool-reproduction-v1]
  timeoutMilliseconds: 120000
  maximumCostUsd: 0
  allowedEffects: [process.execute:test-allowlist]
- id: code-tool-invalid-input
  groupId: defect-tool-invalid-input
  input:
    reproduction: Invoke the tool with a non-string query.
  expected:
    behavior: Reject the request before any network effect.
  rubric: Invalid arguments fail closed at the tool boundary.
  classification: internal
  sourceDigests: [sha256:duckduckgo-tool-reproduction-v1]
  timeoutMilliseconds: 120000
  maximumCostUsd: 0
  allowedEffects: [process.execute:test-allowlist]
