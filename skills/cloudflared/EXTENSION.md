# Cloudflared tunnel

Publish only verified webhook-gateway routes without opening a public listener.
The tunnel token is mounted into this companion alone, and the companion has no
route to other components.
