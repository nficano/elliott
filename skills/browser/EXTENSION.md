# Browser

Render JavaScript-dependent pages in an isolated Chromium companion. Use it
only when a normal fetch cannot read the page. Per-task origin grants control
network access; page text, screenshots, and downloads are untrusted.
